// ABOUTME: Entry point loaded by popup.html — calls init() on DOM ready.
// ABOUTME: Kept separate from popup.js so the logic remains independently testable.
import { init } from './popup.js';

init();
